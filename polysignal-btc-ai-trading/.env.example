# Required
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional — add both to activate the DebateAgent (Groq × OpenAI × Claude judge)
# Without these, the system falls back to the EnsembleAgent automatically.
GROQ_API_KEY=your_groq_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
